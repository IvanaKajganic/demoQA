package bookStoreApp;

import org.openqa.selenium.WebDriver;

public class BookStoreAPI {
    WebDriver driver;
    public BookStoreAPI(WebDriver driver) {
        this.driver = driver;
    }
}
